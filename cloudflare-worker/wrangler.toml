name = "jyotish-ai"
main = "worker.js"
compatibility_date = "2024-11-01"

# Optional: restrict which website may call this worker.
# Set to your GitHub Pages origin, e.g. "https://mumukshtayal.github.io"
[vars]
ALLOWED_ORIGIN = "https://jyotish-sarathi.pages.dev"

# The Groq key is NOT stored here. Set it as an encrypted secret instead:
#   wrangler secret put GROQ_API_KEY
